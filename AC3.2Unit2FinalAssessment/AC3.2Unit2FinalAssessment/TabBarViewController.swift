//
//  TabBarViewController.swift
//  AC3.2Unit2FinalAssessment
//
//  Created by Tyler Newton on 11/18/16.
//  Copyright © 2016 Tyler Newton. All rights reserved.
//

import UIKit

class
TabBarViewController: UIViewController{
//MARK: Properties
    
    override func viewDidLoad() {
        super.viewDidLoad()
    }
//MARK: Actions

}





//
//    @IBOutlet weak var sliderOutlet: UISlider!
//
//    @IBOutlet weak var stepperOutlet: UIStepper!
//
//    @IBOutlet weak var labelOutlet: UILabel!
//
//    @IBOutlet weak var textFieldOutlet: UITextField!
//
//        override func viewDidLoad() {
//            super.viewDidLoad()
//            textFieldOutlet.delegate = self //You must always use 'self' to connect the textfield's delegate to the view controller. 'self' in this case refers to our view controller who has a protocol of UITextFieldDelegate. The UITextFieldDelegate protocol defines the methods that are used to manage the editing and validation of text in our UITextField! Object. The textField calls the methods of its delegate in response to important changes  so that the text field knows how to respond to the input
//
//            labelOutlet.text = "Default Text" //When the application first loads the label will have a default message that we have created here.
//
//        }
//        //The function below asks the delegate if the text field should process the pressing of the return button.The text field calls this method whenever the user taps the return button. Currently the function below sets the label equal to the users input in the text field and then dismisses the keyboard when the user presses enter or return. You can use this method to implement any custom behavior when the button is tapped.
//        func textFieldShouldReturn(_ textField: UITextField) -> Bool {
//            //sets the label equal to the user input in the text field.
//            labelOutlet.text = textField.text
//            //After receiving user input the keyboard will disappear.
//            textField.resignFirstResponder()
//            return true
//        }
//
//        //MARK: Actions
//        //Store your actions here. Stepper, Button, Slider Etc.
//        @IBAction func defaultLabelButton(_ sender: UIButton) {
//            labelOutlet.text = "MAXIMUM LITNESS"
//        }
//        @IBAction func labelStepper(_ sender: UIStepper) {
//            labelOutlet.text = "\(sender.value)"
//    }
//
//        @IBAction func sliderControl(_ sender: UISlider) {
//            labelOutlet.text = "\(sender.value)"
//        }

    
//MARK: Actions

//    @IBAction func sliderAction(_ sender: UISlider) {
//    }
//    
//    @IBAction func stepperAction(_ sender: UIStepper) {
//    }
//    
//    
//    
//}
